{
  "name": "@confchain/contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo \"contracts build skipped in phase0\"",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"solidity lint can be added\"",
    "format": "echo \"solidity format can be added\"",
    "deploy:local": "hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
